jobfwModule.controller('JobDescController', ['$scope', function($scope){
	/*$scope.$watch('jobRequirement.jobdescblock.profile',function(newVal,oldVal){
		console.log($scope.sections[0].isValid);
		if($scope.sections[0].isValid =='true')
		{
			$scope.$apply(function(){$scope.sections[0].heading="Opening for "+newVal;});
		
		}
	});*/
}]);